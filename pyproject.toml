[project]
name = "apis-emulator"
version = "4.5.10"
description = "APIS Emulator"
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "bottle>=0.13",
    "netifaces==0.10.9",
    "numpy",
    "requests",
]

[dependency-groups]
dev = [
    "ipython",
    "ruff",
]

[tool.uv]
package = false